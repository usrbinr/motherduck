% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/secret_fns.R
\name{create_r2_secret}
\alias{create_r2_secret}
\title{Register a Cloudflare R2 secret on a connection}
\usage{
create_r2_secret(
  .con,
  account_id,
  key_id,
  secret,
  name = "r2",
  persistent = FALSE
)
}
\arguments{
\item{.con}{A valid \code{DBI} connection (DuckDB / MotherDuck).}

\item{account_id}{Cloudflare R2 account id (the 32-character hex id).}

\item{key_id}{R2 S3 access key id.}

\item{secret}{R2 S3 secret access key.}

\item{name}{Secret name. Defaults to \code{"r2"}.}

\item{persistent}{Logical; persist the secret to the local store. Defaults to
\code{FALSE} (session-scoped).}
}
\value{
Invisibly, the secret \code{name}.
}
\description{
Creates (or replaces) a DuckDB/MotherDuck secret of \verb{TYPE R2} so subsequent
reads from \verb{r2://} paths authenticate against your Cloudflare R2 bucket. R2 is
S3-compatible; this is a convenience wrapper over the generic
\code{\link[=create_s3_secret]{create_s3_secret()}} that derives the endpoint from your account id.
}
\details{
Requires the \code{httpfs} extension (load it first with
\code{load_extensions(.con, "httpfs")}). The secret is session-scoped by default;
pass \code{persistent = TRUE} to persist it to the local secret store. On
MotherDuck the secret lives in the active session and is used by the cloud
engine to fetch the objects.
}
\examples{
\dontrun{
con <- connect_to_motherduck()
load_extensions(con, "httpfs")
create_r2_secret(
  con,
  account_id = Sys.getenv("R2_ACCOUNT_ID"),
  key_id     = Sys.getenv("AWS_ACCESS_KEY_ID"),
  secret     = Sys.getenv("AWS_SECRET_ACCESS_KEY")
)
DBI::dbGetQuery(con, "SELECT * FROM read_csv_auto('r2://my-bucket/data.csv')")
}
}
\seealso{
\code{\link[=create_s3_secret]{create_s3_secret()}}, \code{\link[=load_extensions]{load_extensions()}}

Other db-manage:
\code{\link[=alter_table_schema]{alter_table_schema()}},
\code{\link[=copy_tables_to_new_location]{copy_tables_to_new_location()}},
\code{\link[=create_database]{create_database()}},
\code{\link[=create_if_not_exists_share]{create_if_not_exists_share()}},
\code{\link[=create_or_replace_share]{create_or_replace_share()}},
\code{\link[=create_s3_secret]{create_s3_secret()}},
\code{\link[=create_schema]{create_schema()}},
\code{\link[=create_table]{create_table()}},
\code{\link[=delete_and_create_schema]{delete_and_create_schema()}},
\code{\link[=delete_database]{delete_database()}},
\code{\link[=delete_schema]{delete_schema()}},
\code{\link[=delete_table]{delete_table()}},
\code{\link[=describe_share]{describe_share()}},
\code{\link[=drop_share]{drop_share()}},
\code{\link[=list_owned_shares]{list_owned_shares()}},
\code{\link[=list_shared_with_me_shares]{list_shared_with_me_shares()}},
\code{\link[=upload_database_to_md]{upload_database_to_md()}}
}
\concept{db-manage}
