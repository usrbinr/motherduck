% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/secret_fns.R
\name{build_secret_sql}
\alias{build_secret_sql}
\title{Build the SQL for a DuckDB/MotherDuck secret}
\usage{
build_secret_sql(.con, name, type, fields, persistent = FALSE)
}
\arguments{
\item{.con}{A valid \code{DBI} connection (used for quoting).}

\item{name}{Secret name (identifier).}

\item{type}{Secret type keyword, e.g. \code{"R2"} or \code{"S3"}.}

\item{fields}{Named list of \code{FIELD = value} pairs. Values are quoted as string
literals; \code{NULL} entries are dropped.}

\item{persistent}{Logical; if \code{TRUE} create a \code{PERSISTENT} secret.}
}
\value{
A \code{glue}/\code{SQL} string.
}
\description{
Internal helper that assembles a \verb{CREATE OR REPLACE SECRET} statement from a
named list of fields. Keyword fields (e.g. \code{TYPE}) are emitted verbatim while
credential fields are quoted as string literals via \code{\link[glue:glue_sql]{glue::glue_sql()}}.
}
\keyword{internal}
